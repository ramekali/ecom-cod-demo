# Mock Backend (placeholder)

This is a minimal mock backend scaffold for local development. Run `npm install`, then `npm run init-db` to initialize the SQLite database, and `npm start` to run the server. Seeded accounts: admin@example.com / adminpass and user@example.com / userpass.