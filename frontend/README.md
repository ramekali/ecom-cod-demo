# Frontend (placeholder scaffold)

This is a minimal scaffold for the frontend. Run `npm install` and `npm run dev` in the frontend directory. Configure VITE_API_BASE to point to the backend (default http://localhost:4000/api).